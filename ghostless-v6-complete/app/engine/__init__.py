"""
Ghostless v6 Decision Engine

Components:
  bayesian.py     — Bayesian Beta trust scoring engine
  pipeline.py     — Unified validation + fraud + scoring pipeline
  feature_store.py — Centralized signal loading (DB + Redis)
  event_store.py  — Immutable event log (Redis Streams + Postgres archive)
"""
